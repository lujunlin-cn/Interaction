# Relay Visual Continuation

Date: 2026-09-25. Runtime service: port 9000.

The supplied Image Relay credential was installed only in ignored `backend/.env`. The configured image route is `https://api-top.com`, model `gpt-image-2.5-sunburst`, and resolution `4K`. Fal Nano Banana was not called.

## Successful assets

- Claire: 2 candidates generated by `IMAGE_GENERATION`, request ID recorded in `relay_character_assets_final.json`; first candidate approved canonical.
- Victor: 4 candidates generated across two `IMAGE_GENERATION` batches, request IDs recorded in `relay_character_assets_final.json`; first candidate from the first batch approved canonical.
- All returned assets report 4096x4096 output dimensions and Relay provenance.

## Blocked assets

The formal `standard-views` workflow attempted four edits per character through `https://api-top.com/v1/images/edits` (8 external attempts total). The Relay returned HTTP 502/network failures. No fallback, mock asset, or Fal image request was used. Leon standard views and Outfit edit were not retried.

The durable usage ledger records the successful generation requests and failed edit attempts without secrets. Visual QA remains PARTIAL until reference views and Outfit edit are available.
